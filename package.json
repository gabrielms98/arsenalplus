{
  "name": "arsenalplus",
  "version": "0.5.1",
  "description": "Extensão de Chrome (não oficial) com melhorias para arsenalsports.com",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build",
    "dev": "vite build --watch",
    "package": "npm run build && mkdir -p releases && cd dist && zip -qr ../releases/arsenalplus-$npm_package_version.zip . && cd .. && ls -t releases/*.zip | tail -n +4 | xargs rm -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": "true",
  "dependencies": {
    "@radix-ui/react-slot": "^1.3.3",
    "@tailwindcss/vite": "^4.3.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
